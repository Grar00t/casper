# Casper repository guidance

Use `AGENTS.md` for engineering rules, `README.md` for supported behavior and commands, and `STRUCTURE.md` for repository inventory/known limitations.

Do not rely on historical build paths, committed binary names, cloud deployment details, fixed test counts, or UI/design notes in older revisions. Verify the current implementation before changing or documenting behavior.
